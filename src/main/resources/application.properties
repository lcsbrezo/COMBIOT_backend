spring.application.name=combiot

# -------------------------------
# SERVER
# -------------------------------
server.port=8100

# -------------------------------
# DATABASE (Supabase/PostgreSQL)
# -------------------------------
spring.datasource.url=jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:6543/postgres?user=postgres.bsyyoofiobwdgaoagwdg&password=ixqNzTqfs1PSqr6z


spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# -------------------------------
# JWT CONFIG
# -------------------------------
app.jwt.secret=9f8d3a7b-5c2e-4a6b-8f1d-2c9e7f4a1b6c-3d5e7f8a9b0c2d4e
app.jwt.expiration=86400000  # 24 horas en ms

# -------------------------------
# MQTT CONFIG (HiveMQ Cloud)
# -------------------------------
#.broker=tcp://<TU_HIVEMQ_HOST>:1883
#mqtt.username=<TU_HIVEMQ_USER>
#mqtt.password=<TU_HIVEMQ_PASSWORD>
#mqtt.clientId=combiot-backend
#spring.datasource.hikari.maximum-pool-size=5
# ... (todas tus propiedades de base de datos, puerto, etc.) ...

# --- Configuración de Actuator para Health Check ---
# Expone el endpoint de salud
management.endpoints.web.exposure.include=health
# Cambia la ruta base de los endpoints de actuator a "/"
management.endpoints.web.base-path=/
# Establece la ruta específica para el health check a "/health"
management.endpoint.health.path=/health

# --- INICIO DE LA MODIFICACIÓN (Opcional pero recomendado) ---
# Muestra siempre los detalles de salud para un mejor diagnóstico
management.endpoint.health.show-details=always
# --- FIN DE LA MODIFICACIÓN ---
# -------------------------------
# LOGGING
# -------------------------------
logging.level.org.springframework=INFO
logging.level.com.iot.combiot=DEBUG
